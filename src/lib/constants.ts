/**
 * Shared constants used across the application
 */

/**
 * Run types available in the system
 */
export const runTypes = [
  { id: "solo", name: "Solo" },
  { id: "co-op", name: "Co-op" },
] as const;

