/**
 * Shared constants for GameDetails components
 */

import { Trophy, Upload, Radio, Download, BarChart3, ShieldAlert } from "lucide-react";

// Icon mapping for header links
export const iconMap: Record<string, React.ComponentType<{ className?: string; size?: number; color?: string }>> = {
  Trophy,
  Upload,
  Radio,
  Download,
  BarChart3,
  ShieldAlert,
};

