/**
 * Centralized Configuration System
 * 
 * This module provides a single entry point for all configuration in the application.
 * Import defaults and config loaders from here.
 */

export { DEFAULT_GAME_CONFIG, DEFAULT_POINTS_CONFIG, DEFAULT_UI_CONFIG, DEFAULT_FEATURE_FLAGS } from "./defaults";
export type { AppConfig } from "./types";

